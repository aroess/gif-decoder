#ifndef COLOR_GUARD
#define COLOR_GUARD

/* this struct is used in the main source file AND the write_ppm source file */
typedef struct color { 
    unsigned char r;
    unsigned char g;
    unsigned char b;
} color;

#endif
